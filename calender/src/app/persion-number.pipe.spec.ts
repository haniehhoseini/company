import { PersionNumberPipe } from './persion-number.pipe';

describe('PersionNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PersionNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
